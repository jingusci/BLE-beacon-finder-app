# v0.4.0

## Fecha de generacion
- vie. 15/05/2026 17:44:46

## APK
- v0.4.0.apk

## Cambios respecto a la version anterior
- Se resolvio el conflicto de merge en `BeaconSupport.kt`.
- Se mantuvo el soporte para audios personalizados por baliza.
- Se conservaron las balizas por defecto V1 y V2, incluyendo la migracion de defaults existentes.
- Se agrego deteccion automatica de audios incluidos en `res/raw` para el selector de audio.
- Se renombro el recurso de audio `baño.mp3` a `banio.mp3` para cumplir con las reglas de nombres de recursos Android.
- Se actualizo la metadata de la app a `versionCode = 400` y `versionName = "0.4.0"`.
